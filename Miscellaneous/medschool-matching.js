/*
Create algorithm that matches med school students for residency
*/

class Student {
  constructor(name) {
    this.name = name;
    this.ranks = {};
  }
}

class School {
  constructor(name, capacity) {
    this.name = name;
    this.capacity = capacity;
    this.spots = [];
    1;
    this.ranks = {};
  }
}

const match = (students, schools) => {};

const toliver = new Student('toliver');
const duke = new School('Duke', 5);
